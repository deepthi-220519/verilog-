//Design module

module full_subtractor(a,b,c_in,diff,borrow);
  input a,b,c_in;;
  output diff,borrow;
  assign diff = a ^ b ^ c_in;
  assign borrow = ((~a) & c_in) | ((~a) & b) | (b & c_in);
endmodule

//Testbench module

